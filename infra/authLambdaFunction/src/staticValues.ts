export const EDGE_REGION: string = "us-east-1";
const PREFIX = "/private-document/auth";
export const COGNITO_REGION: string = `${PREFIX}/region`;
export const USER_POOL_ID: string = `${PREFIX}/auth/user-pool-id`;
export const USER_POOL_CLIENT_ID_PARAM_NAME: string = `${PREFIX}/client-id`;
export const USER_POOL_DOMAIN_PARAM_NAME: string = `${PREFIX}/pool-domain`;
